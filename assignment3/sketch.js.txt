// ⭐ 최종 수정 버전: 좌표 및 중심 문제 해결 ⭐

let centerX = 175; // 🚨 수정: 350px 캔버스의 중앙 (175) 근처로 설정
let centerY = 160; // 🚨 수정: 350px 캔버스의 중심 근처로 설정

function setup() {
  createCanvas(350, 350);
  angleMode(DEGREES);
}

function draw() {
  // 캔버스 크기가 350x350이므로, 캐릭터가 화면 밖으로 나가지 않도록 제한합니다.
  centerX = constrain(centerX, 50, 300);
  centerY = constrain(centerY, 100, 250);

  if (keyIsDown(LEFT_ARROW)) {
    centerX -= 3;
  }
  if (keyIsDown(RIGHT_ARROW)) {
    centerX += 3;
  }
  if (keyIsDown(UP_ARROW)) {
    centerY -= 3;
  }
  if (keyIsDown(DOWN_ARROW)) {
    centerY += 3;
  }

  drawBackground();
  drawHair_Back();
  drawShoulder();
  drawHat_Back();
  drawFace();
  drawHat_Top();
  drawEyes();
  drawNose();
  drawMouth();
  drawHat_Front();
}

// 🚨 drawBackground 함수 내부의 구름 좌표 수정
function drawBackground() {
  noStroke();
  fill('#87CEEB');
  rect(0, 0, width, height / 2 + 50);
  fill('#FFFFFF');
  ellipse(100, 80, 100, 60);
  ellipse(150, 70, 120, 70);
  ellipse(300, 100, 150, 80); // 🚨 X좌표 500 -> 300으로 수정
  ellipse(280, 90, 110, 60);  // 🚨 X좌표 430 -> 280으로 수정
  fill('#66DDAA');
  rect(0, height / 2 + 50, width, height / 4);
  fill('#FFDD00');
  rect(0, height * 3 / 4 + 50, width, height / 4 - 50);
  fill('#90EE90');
  rect(0, height * 3 / 4, width, 50);
}

// (나머지 drawFace, drawEyes 등 함수는 centerX, centerY 변수를 사용하므로 수정 없이 그대로 사용)
// ...
// (모든 함수는 위 수정된 drawBackground() 아래에 그대로 붙여넣습니다.)
// ...